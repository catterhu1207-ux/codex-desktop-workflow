from pathlib import Path
import json, re, subprocess
from frontend_feature_contracts import _extract_function, _with_module_stubs, ContractError
PICKER_PATH = 'webview/assets/composer-project-picker-content-531ef99549e8.js'
PICKER_SHA256 = '6e436c6eee1f234ae9626f0434205dff0f9a2c400285bb02ee6119f29c2e5996'

def run(initial, primary, picker, node='node'):
    functions = {n: _extract_function(primary.decode(), n) for n in ('X7', 'Agt', 'A$', 'iut', 'W_t', 'c_t', 'ogt', 'Tgt')}
    functions.update({n: _extract_function(initial.decode(), n) for n in ('zYr', 'qZx', 'gca')})
    lazy = '\n'.join((_extract_function(picker.decode(), n) for n in ('I', 'L', 'R', 'z')))
    body = '\n'.join(functions.values())
    modules = sorted(set(re.findall('(?<![\\w$.])([\\w$]+)\\.(?:c|jsx|jsxs|Fragment|useRef|useState)\\b', body)))
    prelude = "\nconst assert=(v,m)=>{if(!v)throw Error(m)};\nconst memo=()=>new Array(300).fill(Symbol.for('react.memo_cache_sentinel'));\nconst jsx=(type,props,key)=>({type,props:props||{},key});\nlet search='',cloud=[],localEnabled=true,selected=null,selectedId=null,selectedKind='remote';\nconst intl={formatMessage:(m,v)=>m.defaultMessage.replace('{projectName}',v?.projectName??'')};\nconst Io=()=>intl,F=()=>({isCapable:true,isLoading:false}),Qle=()=>`allowed`;\nconst Ff=Symbol(),Vbe=Symbol(),oge=Symbol(),zse=Symbol(),NC=Symbol(),MSe=Symbol(),Ir=Symbol(),T_=Symbol();\nconst store={get:()=>null,set:()=>{},query:{setData:()=>{}}};\nconst ut=()=>store;\nconst X=(atom)=>atom===Ff?{type:selectedKind,projectId:selectedId}:atom===Vbe?[local]:atom===oge?[local,...remotes]:atom===zse?[]:atom===NC?localEnabled:atom===MSe?{projects:cloud}:null;\nconst wC=(context,descriptor)=>{assert(context===store,'selection uses the store object');selected={descriptor,route:gca(descriptor)}};\nconst zd=(context,value)=>{assert(context===store,'cloud store');selected=value};\nconst ct=value=>value,c='asset',tK={Item:'item',Action:'action'},L$='trigger',EQe='dialog',hy=(s,r)=>String(s).toLowerCase().includes(r.toLowerCase())?1:0;\nconst sgt={default:(a,f)=>a.sort((x,y)=>f(x)-f(y))};\nconst vv=Symbol(),sUt=Symbol(),ea={REMOTE_PROJECTS:Symbol()};\nconst RS=()=>({data:raw,isLoading:false}),cr=atom=>atom===vv?{type:selectedKind,projectId:selectedId}:false;\nconst ids=['11111111-1111-4111-8111-111111111111','22222222-2222-4222-8222-222222222222','050ce269-9b40-44e3-ac49-b67bb85d0c42','48d6231d-156e-493a-a7e3-d3610a38a080'];\nconst names=['project-alpha','project-beta','project-gamma','project-delta'];\nconst raw=ids.map((id,i)=>({id,label:id,hostId:'remote-ssh-discovered:Insolvency',remotePath:'/root/'+names[i]}));\nconst remotes=ids.map((id,i)=>({projectId:id,projectKind:'remote',hostId:raw[i].hostId,path:raw[i].remotePath,label:names[i],gitRepos:[]}));\nconst local={projectId:'local-1',projectKind:'local',label:'Local project',path:'C:/example',gitRepos:[]};\nconst original=JSON.stringify({raw,remotes});\nconst walk=(e,type)=>{if(!e||typeof e!=='object')return null;if(e.type===type)return e;for(const v of Object.values(e.props??e)){if(Array.isArray(v)){for(const a of v){const r=walk(a,type);if(r)return r}}else{const r=walk(v,type);if(r)return r}}return null};\n"
    prelude += '\n'.join((f'const {name}={{c:memo,jsx,jsxs:jsx,Fragment:"fragment",useRef:v=>({{current:v}}),useState:v=>[typeof v==="string"?search:v,()=>{{}}]}};' for name in modules))
    lazy_context = "\nconst z3e=(()=>{const B={c:memo},V={jsx,jsxs:jsx,Fragment:'fragment'},v=()=>intl,a=()=>({projects:cloud,isError:false,isLoading:false}),T=Symbol(),O=ogt,A=Tgt,F='menu',P=tK,g={Message:'message'},f='intl-message',u='spinner',w='icon',d='project-icon',m='checked',E='detail',x='new-project',h='none',l='asset';\n" + lazy + '\nreturn I;})();\n'
    tests = "\nconst render=props=>{let outer=X7(props),middle=outer.type===Agt?Agt(outer.props):outer;return {middle,out:A$(middle.props)}};\nconst results=[];\nfor(let index=0;index<ids.length;index++){\n selectedId=ids[index];search='';\n const rawSelection=zYr();assert(rawSelection.selectedRemoteProject.label===names[index],'raw selected label');\n const fallback=W_t({activeProjectId:selectedId,projects:[],remoteConnections:[],selectedRemoteProject:rawSelection.selectedRemoteProject});\n assert(fallback.label===names[index]&&fallback.hostId===raw[index].hostId&&fallback.path===raw[index].remotePath,'work fallback identity');\n let {middle,out}=render({projectId:selectedId,menuOpen:true});\n assert(out.props.value===names[index]&&out.props['aria-label'].endsWith(names[index]),'visible label and accessible label');\n assert(out.props.tooltipContent==='Change project','tooltip');\n let menu=walk(out,z3e);assert(menu,'actual lazy content route');\n let content=z3e(menu.props),list=walk(content,Tgt);assert(list.props.groups.length===5,'all local and remote options');\n search=names[index];({out}=render({projectId:selectedId,menuOpen:true}));content=z3e(walk(out,z3e).props);list=walk(content,Tgt);\n assert(list.props.groups.length===1&&list.props.groups[0].projectId===selectedId,'human-name search');\n const item=walk(Tgt(list.props),'item');assert(item,'real option rendering');item.props.onSelect();\n assert(selected.route.type==='remote'&&selected.route.projectId===selectedId,'selection routing');\n assert(selected.descriptor.hostId===raw[index].hostId&&selected.descriptor.path===raw[index].remotePath,'host and path preserved');\n results.push({label:out.props.value,aria_label:out.props['aria-label'],route:selected.route});\n}\nsearch='';localEnabled=false;selectedId=ids[2];let {middle,out}=render({});\nassert(middle.props.localProjectOptions.projects.length===4&&out.props.value===names[2],'inherited remote without local capability');\nlet dialog=iut();assert(dialog.props.localProjectOptions.projects.length===5,'dialog remote options');dialog.props.localProjectOptions.onSelectProject(remotes[0]);assert(selected.route.type==='remote','dialog kind preserved');\nlocalEnabled=true;({out}=render({projectId:local.projectId}));assert(out.props.value==='Local project','local label');\nlet content=z3e(walk(out,z3e).props),list=walk(content,Tgt);list.props.onSelectProject(local);assert(selected.route.type==='local','local routing');\ncloud=[{gizmo:{id:'g-p-example',display:{name:'Cloud project'}}}];({out}=render({projectId:'g-p-example'}));assert(out.props.value==='Cloud project','cloud label');\nout.props.onClearProject();assert(selected===null,'clear selection');\nconst explicit=X7({projectId:'g-p-example',onProjectChange:()=>{}});assert(explicit.type===A$&&explicit.props.projectId==='g-p-example','explicit cloud branch preserved');\nremotes[2]={...remotes[2],label:'Renamed SSH'};({out}=render({projectId:ids[2]}));assert(out.props.value==='Renamed SSH','rename rerender');remotes[2]={...remotes[2],label:names[2]};\nselectedKind='local';assert(zYr().selectedRemoteProject===null,'local selection does not acquire remote');\nassert(JSON.stringify({raw,remotes})===original,'source descriptors unchanged');\nprocess.stdout.write(JSON.stringify({status:'passed',results,visible_uuid_count:0,raw_hook:true,actual_wrapper:true,lazy_content:true,search:true,selection:true,local_and_cloud_preserved:true,source_descriptors_unchanged:true}));\n"
    script = prelude + '\n' + body + '\n' + lazy_context + tests
    cp = subprocess.run([node, '-'], input=_with_module_stubs(script, {**functions, 'lazy': lazy}), text=True, capture_output=True, timeout=30)
    if cp.returncode:
        raise ContractError('26.917 Work picker: ' + cp.stderr[-3000:])
    return json.loads(cp.stdout)
