# v0.1.0

Experimental source release for Windows x64 Codex desktop `26.908.9136.0`.

- Builds an independent workflow-mod copy from a matching user-supplied official application directory.
- Verifies Priority recency, planned/pinned/unread/loading indicators, remote-project naming and preserved per-task settings contracts.
- Supports explicit task-data snapshot import into an independent directory.
- Uses the official bundled backend. The optional cross-provider backend remains unqualified for this release.
- Passed two independent maintainer-side renderer runs with 60 seconds of observation each and normal main/backend exit; see `ACCEPTANCE.md`.
