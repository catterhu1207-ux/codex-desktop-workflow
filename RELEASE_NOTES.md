# v0.2.2

Adds exact Codex Desktop 26.915.4065.0 support. The public workflow builds an independent copy with the unchanged official backend and retains 26.915.3509.0 and 26.908.9136.0 profiles.

Updated contracts cover attention colors and pinned precedence, start-time ordering and live refresh, readable remote project labels and routing, the Work picker, history rendering, file drop and the exact renderer-proof route. Unknown or changed package bytes remain rejected.

See ACCEPTANCE.md for public build and isolated-launch evidence. Private-maintenance supplemental resume checks are reported separately; they do not qualify a patched backend for this public installer. User data, authentication, official binaries and raw runtime logs are excluded from the release.
