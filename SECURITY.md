# Security

Report security issues privately through GitHub Security Advisories.

The tool rejects unrecognized package hashes and writes only to a new target directory. Keep the official source and imported task-data source unchanged. Review generated JSON reports before sharing them because local artifact reports contain paths to user-selected directories.

